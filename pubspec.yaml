name: flow
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc:
  flow_annotation:
    path: ../flow_annotation/
  firebase_core: ^2.9.0
  firebase_auth: ^4.4.0
  google_sign_in:
  cloud_firestore:
  hydrated_bloc:
  path_provider:
  http:
  firebase_messaging:
  flutter_code_editor: 0.2.18

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner:
  flow_generator:
    path: ../flow_generator/

flutter:

  uses-material-design: true

  assets:
    - images/
